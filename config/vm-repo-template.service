[Unit]
Description=VM Package Repo Exposure

[Service]
ExecStart=${KUBECTL} \
            --kubeconfig $HOME/.kube/config \
            --context active-cluster-m2 \
            port-forward -n istio-system service/vmgateway 443:443 --address 0.0.0.0

Restart=on-failure
RestartSec=5s

[Install]
WantedBy=multi-user.target